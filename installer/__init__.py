"""Installer subcommands for the ``coros-mcp`` CLI.

- ``installer.wizard``       — orchestrates the end-to-end setup flow.
- ``installer.assistants``   — detection + config writers for each AI assistant.
- ``installer.smoke``        — post-install smoke test against the installed server.
- ``installer.regions``      — timezone → region default heuristic.
"""
